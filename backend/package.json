{
  "name": "GameCreator_Backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "(if exist dist rmdir /s /q dist) && tsc && (cd ../frontend && npm run build && cd ../backend)",
    "debug": "(if exist dist rmdir /s /q dist) && tsc && (cd ../frontend && npm run build && cd ../backend) && node dist/index.js"
  },
  "keywords": [],
  "author": "Branden Immerzeel",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "3.4.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  }
}
